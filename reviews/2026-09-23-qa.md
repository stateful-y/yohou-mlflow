# QA report: 2026-09-23

Scope: source, docstrings, tests, design, surface, docs, assets / all | Mode: full (no prior SHA)
Commit: e709230
Findings: 27 new, 0 known, 0 accepted-suppressed | Refuted in verification: 9
Simplification: 2 findings, net reduction ~2 (one imported name, one deferred-import indirection)

Run notes: the yohou QA chassis (`qa.md` and its seven spokes, in the yohou repository) was adapted to this package. Manifests were built from `yohou_mlflow.__all__` and the package import graph; categories that depend on yohou-only machinery (systematic check suites, glossary, `create-yohou-*` skills, estimator tags, `compose/`, `base/`) were skipped. Template-owned files were out of scope except in the assets dimension, where findings route to python-package-copier. All seven dimensions ran in one workflow (13 reviewers, 2 consistency passes, 4 verifier batches) because the package is small.

## High

| ID | Status | Category | File | Claim | Evidence | Suggestion |
|---|---|---|---|---|---|---|
| `docs:examples/registry_loop.py:tempdir-leaves-nothing-behind-false` | NEW | accuracy | `examples/registry_loop.py` | The notebook tells the reader the temp-directory registry "leaves nothing behind," but the code never deletes the directory it creates, so the claim is false. | examples/registry_loop.py:119-120 markdown says "We keep the registry in a temporary directory, so this notebook leaves nothing behind, and log the forecaster as version 1"; line 129 does `workdir = Path(tempfile.mkdtemp())` and nothing in the file ever calls `shutil.rmtree`, uses `tempfile.TemporaryDirectory()`, or registers cleanup. Ran the full cell se... | Either replace `tempfile.mkdtemp()` with `tempfile.TemporaryDirectory()` used as a context manager (or add an explicit `shutil.rmtree(workdir)` at the end) so the claim is true, or rewrite the sentence to say the directory is temporary but not auto-removed. |
| `docs:docs/pages/reference/saved-model-format.md:example-mlmodel-has-stale-code-key` | NEW | accuracy | `docs/pages/reference/saved-model-format.md` | The example MLmodel YAML shows a `code: null` key under `flavors.yohou`, but a real save_model run's MLmodel file has no `code` key there at all; the key was already dropped from the table above the example when code_paths support was removed, and only the example was left stale. | docs/pages/reference/saved-model-format.md:34 shows `code: null` inside the flavors.yohou example. Running `yohou_mlflow.save_model` in this repo (scratch script under /tmp/.../qa-ym/docs-reference/check_mlmodel.py) and reading the written MLmodel file shows the `yohou:` block has only components/default_prediction_type/forecaster_class/forecaster_type/fo... | Delete the `code: null` line from the example YAML so it matches the table above it, which no longer documents a `code` key. |
| `assets:.claude/skills/update-from-template/SKILL.md:conflict-rej-destroys-local-content` | NEW (template-owned) | stale-reference | `.claude/skills/update-from-template/SKILL.md` | Step 4 still instructs `copier update --trust --conflict rej --skip-answered`, but the upstream template (python-package-copier) removed that flag in commit 0b0411c (PR #324) because `--conflict rej` was measured to silently destroy local content (it turned a live 'Compat tests' pin set into the template's disabled placeholder and dropped `lfs: true`) and to leave digest-pinned `uses:` lines un... | yohou-mlflow/.claude/skills/update-from-template/SKILL.md:95: `copier update --trust --conflict rej --skip-answered` vs python-package-copier/template/.claude/skills/update-from-template/SKILL.md:95: `copier update --trust --skip-answered` | Routes to the template (this skill is shipped by python-package-copier, not owned by this repo): re-sync this file from the current template (which now prescribes the default inline merge, no `--conflict rej`) on the next `copier update`/fan-out; cross-linked with the same staleness in references/conflict-resolution... |
| `assets:.claude/skills/update-from-template/references/conflict-resolution.md:conflict-rej-destroys-local-content` | NEW (template-owned) | stale-reference | `.claude/skills/update-from-template/references/conflict-resolution.md` | This reference still frames `--conflict rej` as the recommended mode ("## Why --conflict rej", "Use `--conflict rej`") and describes only the `.rej`-based tier-resolution workflow. The current template version of this same file replaces that whole section with "## Use the default inline merge, NOT --conflict rej", documenting the measured data-loss (5 `.rej` files vs 0, 49→26 vs 49→49 digest-pi... | yohou-mlflow/.claude/skills/update-from-template/references/conflict-resolution.md:19-36 ("## Why --conflict rej ... Use `--conflict rej`. This produces...") vs python-package-copier/template/.claude/skills/update-from-template/references/conflict-resolution.md:19-59 ("## Use the default inline merge, NOT --conflict rej ... Do not pass `--conflict rej`") | Routes to the template: re-sync this file from the current template on the next skills refresh so an agent following it does not run the destructive flag; same root cause as the SKILL.md finding above. |

## Medium

| ID | Status | Category | File | Claim | Evidence | Suggestion |
|---|---|---|---|---|---|---|
| `docstrings:src/yohou_mlflow/_persistence.py:save-model-raises-omits-no-supported-types` | NEW | bidirectional | `src/yohou_mlflow/_persistence.py` | save_model's Raises section documents ValueError only for a signature clash, but the code also raises a plain ValueError from `default_prediction_type` when the forecaster supports none of the three prediction types, and that path is not mentioned. | src/yohou_mlflow/\_persistence.py:147-148: "ValueError\n If ``signature`` has an input schema or redefines a flavour param." while src/yohou_mlflow/\_persistence.py:172 calls `default_type = default_prediction_type(supported)`, and src/yohou_mlflow/\_pyfunc.py:85-86 shows that function raising `ValueError(f"The forecaster supports none of the prediction type... | Add a second ValueError bullet to save_model's Raises section: 'If the forecaster supports none of the point, interval or class_proba prediction types.' |
| `docs:docs/pages/reference/saved-model-format.md:save-checks-order-contradicts-itself` | NEW | accuracy | `docs/pages/reference/saved-model-format.md` | The 'Save checks' section states save_model applies its checks "after writing forecaster.skops", but the table's own first row says that check is 'raised before anything is written', and the code confirms the fitted-forecaster check runs before any directory or file is created. | docs/pages/reference/saved-model-format.md:131-136: "`save_model` applies these checks after writing `forecaster.skops` and removes the directory if one fails:" followed by "\| Forecaster is a fitted yohou forecaster \| `TypeError`, `NotFittedError` (raised before anything is written) \|". Contradicted by src/yohou_mlflow/\_persistence.py:164-177, where `i... | Reword the intro so it doesn't claim all four checks run after writing forecaster.skops, e.g. "save_model applies these checks; the first before anything is written, the rest after writing forecaster.skops and removing the directory if one fails". |

## Low

| ID | Status | Category | File | Claim | Evidence | Suggestion |
|---|---|---|---|---|---|---|
| `source:src/yohou_mlflow/_persistence.py:zoneinfo-message-ignores-actual-cause` | NEW | error-handling | `src/yohou_mlflow/_persistence.py` | \_verify_round_trip attributes any load failure to the ZoneInfo gap whenever the forecaster's type list merely contains zoneinfo.ZoneInfo, instead of checking whether the actual exception is related to it, so an unrelated load failure on a forecaster that happens to hold a time zone gets a false explanation. | \_persistence.py:252-264: `except Exception as exc:\n if "zoneinfo.ZoneInfo" in types:\n msg = (\n "The forecaster was fitted on time-zone-aware data, and the installed skops " ...` , the branch keys off `types` (the file's recorded type list from `skops.io.get_untrusted_types(data=data)`), not off `exc`, so any exception at all (corrupt bytes, an unrelate... | Only attribute the failure to the ZoneInfo gap when the exception itself names zoneinfo/ZoneInfo (e.g. inspect `type(exc)`/`str(exc)`, or catch the specific TypeError skops raises for it), and fall back to the generic message otherwise. |
| `source:src/yohou_mlflow/_persistence.py:field-default-unnecessary-for-immutable-tuple` | NEW | needless-complexity | `src/yohou_mlflow/_persistence.py` | CompatibilityReport's two tuple-typed fields use `field(default=())`, which dataclasses only requires for mutable defaults; an immutable empty tuple can be a plain default. | \_persistence.py:497-498: `version_mismatches: tuple[VersionMismatch, ...] = field(default=())` and `untrusted_types: tuple[str, ...] = field(default=())`. | Replace both with `= ()` directly (dataclasses only needs `field(default_factory=...)` for mutable defaults like list/dict/set); this drops the now-unused `field` import too, one fewer name imported. |
| `source:src/yohou_mlflow/_exceptions.py:needless-local-import-in-versionmismatcherror` | NEW | needless-complexity | `src/yohou_mlflow/_exceptions.py` | VersionMismatchError.\__init__ imports describe_mismatches locally inside the method, but \_versions.py never imports \_exceptions.py, so there is no circular-import reason for the deferred import and it could be a normal top-level import. | \_exceptions.py:61-62: `def __init__(self, mismatches: Sequence[VersionMismatch]) -> None:\n from yohou_mlflow._versions import describe_mismatches`, while the module already imports `VersionMismatch` from the same module under `TYPE_CHECKING` (lines 8-9) with no runtime import currently required. | Move `from yohou_mlflow._versions import describe_mismatches` to the module's top-level imports alongside the existing TYPE_CHECKING import of VersionMismatch, removing the per-call deferred-import indirection. |
| `tests:tests/test_persistence.py:default-allowlist-duplicates-round-trip` | NEW | near-duplicate | `tests/test_persistence.py` | test_default_allowlist_needs_no_extra_types (143-146) is functionally identical to test_round_trip (53-56) at case="point": both build PointReductionForecaster(estimator=Ridge()) via make_case("point"), save it, load it and assert predict().equals(...), so it adds no fault-detection surface the parametrized round-trip test doesn't already cover, while its section heading implies dedicated trust... | test_persistence.py:143-146 `def test_default_allowlist_needs_no_extra_types(point_case: Case, tmp_path: Path) -> None: ... loaded = yohou_mlflow.load_model(str(_saved(point_case, tmp_path))); assert loaded.predict().equals(point_case.forecaster.predict())` vs test_persistence.py:53-56 `def test_round_trip(case: Case, tmp_path: Path) -> None: ... loaded =... | Delete test_default_allowlist_needs_no_extra_types; test_round_trip already exercises the identical save/load/predict-equals path for the point family via the case fixture. |
| `tests:tests/test_persistence.py:family-tuple-duplicates-families-constant` | NEW | near-duplicate | `tests/test_persistence.py` | test_every_family_saves_within_policy hardcodes the five-family tuple instead of importing conftest.FAMILIES, so the two lists can silently drift apart, and its per-family untrusted-type check is largely subsumed by the case-parametrized round-trip tests, which already exercise save_model's identical untrusted_outside_policy check for every family and would fail with UntrustedTypesError if any... | test_persistence.py:429-433 `def test_every_family_saves_within_policy() -> None: ... for family in ("point", "interval", "class_proba", "panel", "composite"): types = skops.io.get_untrusted_types(...); assert untrusted_outside_policy(types) == [], family` versus conftest.py:44 `FAMILIES = ("point", "interval", "class_proba", "panel", "composite")`. | Iterate conftest.FAMILIES instead of a hardcoded literal tuple to remove the drift risk. |
| `tests:tests/test_examples.py:registry-loop-equals-checks-never-asserted` | NEW | vacuous-assertion | `tests/test_examples.py` | The only check test_notebook_runs_without_error makes for examples/registry_loop.py is the subprocess return code; the notebook's own round-trip identity checks are computed as cell return values and never asserted, so marimo does not fail the run when they are False. | tests/test_examples.py:41 `assert result.returncode == 0, (...)` is the sole assertion. examples/registry_loop.py:158 `loaded.predict().equals(forecaster.predict())` and examples/registry_loop.py:205 `{"observed up to": latest.observed_time_, "same forecast": latest.predict().equals(forecast)}` are cell expressions returned for display, not asserted; a ma... | Add `assert loaded.predict().equals(forecaster.predict())` and `assert latest.predict().equals(forecast)` in the respective cells so a broken load/observe/predict round trip fails the notebook (and this test) instead of silently displaying False. |
| `tests:tests/conftest.py:local-ridge-case-duplicates-fit-observe` | NEW | fixture-sprawl | `tests/conftest.py` | local_ridge_case re-implements the same catch_warnings/fit/observe block make_case already runs for every family, instead of reusing it with a swapped estimator. | tests/conftest.py:124-127 (make_case): `with warnings.catch_warnings(): warnings.simplefilter("ignore"); forecaster.fit(data[:FIT_END], forecasting_horizon=horizon); forecaster.observe(data[FIT_END:OBSERVED_END])` is duplicated verbatim at tests/conftest.py:168-171 inside local_ridge_case. | Give `_build` an estimator override (or extract a `_fit_and_observe(forecaster, data, horizon)` helper) so `local_ridge_case` calls the same fit-and-observe path `make_case` uses instead of repeating the block. |
| `tests:tests/test_pyfunc.py:groups-restricts-panel-blind-to-observe-filtering` | NEW | bidirectional | `tests/test_pyfunc.py` | test_groups_restrict_the_panel cannot detect whether the pyfunc wrapper actually threads `groups` into the copy's `observe()` call: its native comparison observes all groups and only restricts predict, yet still equals the wrapper's output because group 'a' forecasts independently of whether b/c were observed. | tests/test_pyfunc.py:134 compares against `_observed_copy(panel_case).predict(groups=["a"])`, whose `_observed_copy` (test_pyfunc.py:29-32) calls `forecaster.observe(case.next_rows)` with no `groups` argument, while src/yohou_mlflow/\_pyfunc.py:279-286 passes `groups=groups` into the copy's own `forecaster.observe(...)` call as well. | Add an assertion that exercises the observe-side effect directly (e.g. compare against a copy observed with `groups=["a"]` explicitly, or inspect a non-requested group's state) instead of relying on per-group independence to make the two call sequences coincide by accident. |
| `design:src/yohou_mlflow/_persistence.py:private-mlflow-helper-reliance-untracked` | NEW | dependency-direction | `src/yohou_mlflow/_persistence.py` | save_model/log_model/load_model depend on 11 underscore-prefixed (private, unversioned) symbols from three mlflow.utils submodules, and this reliance is disclosed only in a module docstring, not tracked in design.md's Risks/Trade-offs registry, while the runtime constraint mlflow-skinny>=3.0,<4 permits releases beyond the two the docstring says were checked. | \_persistence.py:7-9 states "The `mlflow.utils` helpers imported below are private to MLflow... They were checked to exist with the same signatures on mlflow-skinny 3.0.0 and 3.16.1."; lines 29-43 import \_CONDA_ENV_FILE_NAME, \_CONSTRAINTS_FILE_NAME, \_PYTHON_ENV_FILE_NAME, \_REQUIREMENTS_FILE_NAME, \_process_conda_env, \_process_pip_requirements, \_PythonEnv, \_... | Add a Risks/Trade-offs entry in design.md naming this private-API reliance and the versions it was checked against, and add a CI job (or Renovate range test) that exercises save_model/load_model against the floor and ceiling of the mlflow-skinny range so an upstream rename is caught before release rather than silent... |
| `surface:docs/pages/reference/saved-model-format.md:format-version-constant-not-cross-referenced` | NEW | export-doc-mismatch | `docs/pages/reference/saved-model-format.md` | FORMAT_VERSION is exported in yohou_mlflow.\__all__ and its value is described extensively in this doc, but the doc never names the export the way sibling constants documented in the same file do. | saved-model-format.md:4 'Format version `1.0`.' and :21 '`format_version` \| string \| `"1.0"`' state the value with no reference to `yohou_mlflow.FORMAT_VERSION`, while the file's own convention (lines 89-90 'The prefixes and exact names are exported as `yohou_mlflow.TRUSTED_TYPE_PREFIXES` and `yohou_mlflow.TRUSTED_TYPES`.' and line 105 'The rules are ex... | Add a one-line 'exported as `yohou_mlflow.FORMAT_VERSION`' note next to the format-version table or the '## Format version' section, matching the callout already used for TRUSTED_TYPE_PREFIXES/TRUSTED_TYPES/VERSION_RULES. |
| `surface:docs/pages/reference/pyfunc-contract.md:prediction-types-constant-not-cross-referenced` | NEW | export-doc-mismatch | `docs/pages/reference/pyfunc-contract.md` | PREDICTION_TYPES is exported in yohou_mlflow.\__all__ and its three values are spelled out in the params table, but unlike its sibling constants INPUT_KEYS and PARAM_NAMES from the same \_pyfunc.py module, the doc never names the export. | pyfunc-contract.md:46 lists '`prediction_type` \| string \| ... \| One of `point`, `interval`, `class_proba`' without citing `yohou_mlflow.PREDICTION_TYPES`, while line 28 ('subset of `yohou_mlflow.INPUT_KEYS`') and line 42 ('these params (`yohou_mlflow.PARAM_NAMES`)') explicitly name their exports. | Reference `yohou_mlflow.PREDICTION_TYPES` next to the prediction_type row, consistent with how INPUT_KEYS and PARAM_NAMES are cited two lines above. |
| `docs:docs/pages/explanation/concepts.md:load-back-overhead-40-percent-unqualified` | NEW | accuracy | `docs/pages/explanation/concepts.md` | The unqualified statement that the load-back check adds about 40 percent to a save contradicts the Ridge row of the same table, where the check roughly triples save time. | concepts.md:143-147 table shows `Ridge \| ... \| 0.03 s / 0.01 s` ((0.03-0.01)/0.01 = 200%) and `HistGradientBoostingRegressor \| ... \| 2.22 s / 1.57 s` ((2.22-1.57)/1.57 = ~41%); the prose then reads 'The load-back check adds about 40 percent to a save, and the per-call copy about a quarter of a generic prediction for the larger model.' Reproduced indep... | Move 'for the larger model' to the front of the sentence (e.g. 'For the larger model, the load-back check adds about 40 percent...') so both clauses are visibly scoped to that row, since the Ridge row shows roughly a 3x overhead. |
| `docs:docs/pages/explanation/concepts.md:missing-link-trust-third-party-howto` | NEW | cross-references | `docs/pages/explanation/concepts.md` | The passage explaining that caller-supplied types must be named on every save and load never links to the how-to guide that walks through exactly that procedure, even though that guide links back to this page. | concepts.md:74-75: 'Anything else, such as an estimator class from your own project, must be named by the caller, when saving and again on every load.' has no link, while docs/pages/how-to/trust-third-party-estimators.md's 'See Also' section already links forward to 'Concepts (../explanation/concepts.md): why the model file cannot say what to trust.' | Add a link to `../how-to/trust-third-party-estimators.md` on that sentence, mirroring the how-to page's existing backlink. |
| `docs:docs/pages/explanation/concepts.md:missing-link-migrate-from-pickle-howto` | NEW | cross-references | `docs/pages/explanation/concepts.md` | The 'The problem with pickle' section, which motivates moving off MLflow's generic pickle-based storage, never links to the how-to guide that covers performing that exact migration. | concepts.md:10-24 discusses pickling forecasters through 'MLflow's generic Python model' and its failure modes, but docs/pages/how-to/migrate-from-pickle.md ('How to Move Pickled Forecasters to the Yohou Flavour') is never referenced anywhere in concepts.md, including the 'Connections' section. | Add `../how-to/migrate-from-pickle.md` to the 'Connections' list or inline in 'The problem with pickle' section. |
| `docs:docs/pages/reference/saved-model-format.md:python-function-table-missing-python-version-key` | NEW | accuracy | `docs/pages/reference/saved-model-format.md` | The `flavors.python_function` key table lists only loader_module, config and env, but a real saved model's MLmodel file also carries a `python_version` key under `flavors.python_function` that the table omits. | docs/pages/reference/saved-model-format.md:60-64 table has 3 rows (loader_module, config, env). A live save_model run's MLmodel file's `python_function:` block includes `python_version: 3.14.1` alongside config/env/loader_module (verified via scratch script under /tmp/.../qa-ym/docs-reference/check_mlmodel.py). | Add a `python_version` row to the table describing the interpreter version MLflow records at save time. |
| `docs:docs/pages/reference/saved-model-format.md:component-path-check-omits-empty-name-case` | NEW | accuracy | `docs/pages/reference/saved-model-format.md` | The doc says a component file name raises YohouMlflowError only when it 'contains a path separator, or is `.` or `..`', but the code also raises for a missing/empty component name, which the doc never mentions. | docs/pages/reference/saved-model-format.md:75-76: "A component file name that contains a path separator, or is `.` or `..`, raises `YohouMlflowError`." vs. src/yohou_mlflow/\_persistence.py:470 `if not name or os.path.basename(name) != name or name in {".", ".."}:` which also raises when `name` is empty. | Extend the sentence to cover the empty case, e.g. "A component file name that is empty, contains a path separator, or is `.` or `..`, raises YohouMlflowError." |
| `docs:docs/pages/reference/saved-model-format.md:trust-policy-skops-default-row-omits-scipy` | NEW | accuracy | `docs/pages/reference/saved-model-format.md` | The trust policy's 'skops default' row says skops trusts types on its own 'including most scikit-learn estimators and numpy types', omitting scipy, which the code comment it paraphrases explicitly names. | docs/pages/reference/saved-model-format.md:84: "skops default \| Types skops trusts on its own, including most scikit-learn estimators and numpy types" vs. src/yohou_mlflow/\_trust.py:5: "(most scikit-learn estimators, numpy, scipy) never reach this policy". | Add scipy to the row: "including most scikit-learn, numpy and scipy types". |
| `assets:CLAUDE.md:still-reads-as-unedited-template-seed` | NEW | convention-drift | `CLAUDE.md` | CLAUDE.md is byte-identical to the rendered template seed (template/CLAUDE.md.jinja with project_name/description substituted) with zero package-specific edits, despite the file's own instruction ("## What to record here") that it should hold facts a newcomer cannot derive from the code. It records nothing about yohou-mlflow's actual behavior: the fixed trust policy (trusts the `sklearn.` prefi... | yohou-mlflow/CLAUDE.md:1-41 matches python-package-copier/template/CLAUDE.md.jinja:1-41 verbatim aside from Jinja substitution; CLAUDE.md:36-40 ("## What to record here ... why a dependency is pinned, which assumptions this project breaks relative to the template, what a failing check actually means") is itself unaddressed. | Project-owned, not a template-routing issue: add a short package-specific section recording the trust policy, the code_paths removal/import-path security fix, and the strict version-check behavior, so the file does what its own "What to record here" section asks. |
| `docs:docs/pages/how-to/migrate-from-pickle.md:missing-use-it-when-clause` | NEW | opening-pattern | `docs/pages/how-to/migrate-from-pickle.md` | The opening paragraph omits the 'Use it when...' scenario sentence that every other how-to page, and the diataxis-howto-writer template, pairs with the 'This guide shows you how to...' sentence. | migrate-from-pickle.md:3-4 reads 'This guide shows you how to re-save forecasters you stored with pickle, for example through MLflow's generic Python model, as yohou-flavour models.' with no follow-on 'Use it when...' sentence, while check-before-deploying.md:3-4, predict-with-pyfunc.md:3-4, trust-third-party-estimators.md:3-4 and keep-the-registry-small.... | Add one sentence after the opening line stating the real-world scenario, e.g. 'Use it when a job still calls mlflow.sklearn.log_model or a custom PythonModel wrapper for a yohou forecaster.' |
| `docs:docs/pages/how-to/migrate-from-pickle.md:see-also-omits-reference-explanation-links` | NEW | crosslink-pattern | `docs/pages/how-to/migrate-from-pickle.md` | This page's See Also section links only to another how-to page, breaking the how-to -> reference/explanation cross-link pattern the other four how-to pages follow. | migrate-from-pickle.md's See Also section contains a single bullet, '- How to check that a registered model will load (check-before-deploying.md)', with no reference or explanation link, whereas check-before-deploying.md, predict-with-pyfunc.md and trust-third-party-estimators.md each link to both a reference page (../reference/saved-model-format.md or .... | Add a See Also bullet linking to ../explanation/concepts.md (or ../reference/saved-model-format.md, already cited in this page's Troubleshooting section) to match the other how-to pages' pattern. |
| `docs:docs/pages/how-to/migrate-from-pickle.md:prerequisite-bullet-full-sentences` | NEW | prerequisites-format | `docs/pages/how-to/migrate-from-pickle.md` | The first Prerequisites bullet is written as two complete, period-terminated sentences, unlike every other prerequisites bullet across the five how-to pages, which are single sentence fragments with no terminal period. | migrate-from-pickle.md:7-8: '- The environment that saved the pickles, with the same yohou, scikit-learn and polars versions. A pickle may not load, or may load and misbehave, anywhere else.' contains two full stops, while e.g. check-before-deploying.md:8 '- A forecaster registered with `yohou_mlflow.log_model`' and keep-the-registry-small.md:8 '- A regis... | Split the bullet into a single fragment (drop the trailing period) and move the caveat about pickles not loading elsewhere into prose after the Prerequisites list, matching the fragment style used by the other bullets. |

## Per-dimension summaries

| Dimension | Category | High | Medium | Low |
|---|---|---|---|---|
| assets | convention-drift | 0 | 0 | 1 |
| assets | stale-reference | 2 | 0 | 0 |
| design | dependency-direction | 0 | 0 | 1 |
| docs | accuracy | 2 | 1 | 4 |
| docs | cross-references | 0 | 0 | 2 |
| docs | crosslink-pattern | 0 | 0 | 1 |
| docs | opening-pattern | 0 | 0 | 1 |
| docs | prerequisites-format | 0 | 0 | 1 |
| docstrings | bidirectional | 0 | 1 | 0 |
| source | error-handling | 0 | 0 | 1 |
| source | needless-complexity | 0 | 0 | 2 |
| surface | export-doc-mismatch | 0 | 0 | 2 |
| tests | bidirectional | 0 | 0 | 1 |
| tests | fixture-sprawl | 0 | 0 | 1 |
| tests | near-duplicate | 0 | 0 | 2 |
| tests | vacuous-assertion | 0 | 0 | 1 |

Refuted in verification, by dimension: design 3, docstrings 3, source 3.

## Appendix: accepted findings (suppressed)

None. This repository has no `reviews/accepted.md`.

## Follow-up

- The two high `assets` findings are in template-owned skill files. The template already fixed them on `main` (python-package-copier #324); this repository receives the fix with the next template release and fan-out.
- Cluster related unaddressed findings into OpenSpec changes via /opsx:propose (manual step, never automatic).
- Cluster simplification findings into their own deletion-oriented changes per subsystem, never mixed into defect-fix changes.
- To accept a finding as a deliberate tradeoff, add its ID and a one-line justification to reviews/accepted.md.
